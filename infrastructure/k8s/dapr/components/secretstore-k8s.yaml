# Dapr Secret Store Component - Kubernetes Secrets
# Feature: 005-oke-dapr-kafka-infra
# Tasks: T095, T096

apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: kubernetes-secrets
  namespace: todo-app
  labels:
    app.kubernetes.io/name: dapr
    app.kubernetes.io/component: secretstore
spec:
  type: secretstores.kubernetes
  version: v1
  metadata: []
  # No additional metadata needed - uses service account permissions

---
# Alternative: Local file secret store (for development)
# This can be used when testing locally without Kubernetes
# apiVersion: dapr.io/v1alpha1
# kind: Component
# metadata:
#   name: local-secrets
#   namespace: todo-app
# spec:
#   type: secretstores.local.file
#   version: v1
#   metadata:
#     - name: secretsFile
#       value: "/secrets/secrets.json"
#     - name: nestedSeparator
#       value: ":"
